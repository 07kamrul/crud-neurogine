
server.port=8084

#LOCAL

database_path=localhost:27017
connected_db_name=neurogine
connected_db_username=mongodb
connected_db_password=k@mruloo7

spring.jpa.show-sql=false
spring.jpa.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create

spring.datasource.url=jdbc:mongodb://${database_path}/${connected_db_name}
#spring.datasource.url=jdbc:postgresql://localhost:5432/vision
spring.datasource.username=${connected_db_username}
spring.datasource.password=${connected_db_password}
spring.datasource.driverClassName=org.mongodb.Driver


# -------------- FILE UPLOAD
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.assets=assets
file.uploads=${file.assets}/uploads
file.images=images
image.upload.path=resources/assets/images/uploads


